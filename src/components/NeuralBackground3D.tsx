"use client";

// Three.js/R3F background removed in Phase 1 redesign.
// InferenceCanvas (Canvas 2D) is the new visual signature.
// File preserved so dynamic imports in page.tsx and blog/layout.tsx remain intact.
// Three.js packages can be removed from package.json in Phase 3.

export default function NeuralBackground3D() {
  return null;
}
